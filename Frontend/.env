# ─── API ──────────────────────────────────────────────────────────────────────
# Base URL consumed by httpServices.js via import.meta.env.VITE_API_BASE_URL
# The axios instance appends "/api" automatically, so do NOT add it here.
VITE_API_BASE_URL=http://localhost:3000
